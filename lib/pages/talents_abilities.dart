import 'package:flutter/material.dart';

class TalentInfo extends StatelessWidget{
    const TalentInfo({super.key});

    // TODO: implement the data fetching stuff.
    @override
    Widget build(BuildContext context){
        return const Center(
            child: Text("DUMMY")
        );
    }
}